# 2026-06-07 工作记录

> 状态: v1.3 修复完成, 性能优化讨论启动, 明天继续

## 今日完成 (v1.3 收尾)

### 数据层修复
- **角色 attackRange → 0**: 19 角色全部归 0
  - 用户原话: "初期完全靠武器, 之后玩家买道具来增加射程"
  - 视口 800px 实测: 枪手+手枪 670→320, 弓手+强弓 680→320, 弩手+弩 700→320
- **attackRangeMult 字段彻底删除**:
  - `csv/weapons.csv`: header + 52 数据删列, 注释更新
  - `scripts/csv2json.cjs` `weaponSchema`: 删字段
  - `src/data/weapons.json` 48 字段 → 47 字段
  - `index.html` data-bundle `?v=2026060704` → `?v=2026060708`
  - 保留 `ui.js:1570` 的 `item.mods.attackRangeMult` (道具 mod 字段, 与武器 CSV 无关)

### Bug 1 修复 (Brotato 命中规则)
- **根因**: `_findNearestTarget` 用 `weaponPos` (头顶 128px) 搜索, `_tickMeleeHitDetection` 用 `player` 判定 — 中心点不一致
- **改 `player.js:341, 348`**: 搜索中心 → `p` (Brotato 规则)
- **改 `player.js:519`**: 锥形 thrust 半角 π/36 → **π/72 (5°总角)**, sweep π/2 (180°总角) 不变
- **注意**: `cone` 是**半角**, 总角=2×半角

### Bug 2 修复 (Brotato 无推人)
- **改 `enemy.js:67-70`**: chaser 接触删除 `player.knockbackX/Y = ±200` 两行
- **保留**: 爆炸击退 (boss.js + enemy.js:232-233) — 是 boss 主动技能/自爆 AoE

### 新测试
- `test/unit/attack-brotato.test.js`: 16 个测试 AT1-AT16
  - AT1-AT4: 搜索中心=角色
  - AT5-AT14: 锥形角度 + 弧长公式
  - AT15-AT16: chaser 接触不推人
- 测试基线 434/15 守恒, 期望本轮改完后 450/15

## 性能优化讨论 (今日启动, 明日继续)

详见 `docs/系统设计/性能预算与渲染优化方案.md`

**问题**: 1000 怪能否支持? → **当前架构极限 500-600 怪, 1000 怪需要换底层**

**用户选择**: 方案 A — Canvas 2D 优化 (短期推到 600 怪, 不引依赖)
**未来**: 1000+ 怪时切换 PixiJS (比 LeaferJS 更适合游戏)

**方案 A 待实施**:
- 普通怪 shadowBlur = 0
- 同色怪批处理
- 视口剔除
- HP 条边框省
- 新建 `test/unit/performance.test.js`

## 明日继续 (Pending Items)

### 紧急 (v1.3 收尾)
1. bump `index.html` 中改动的 `cyberblade/player.js` 和 `engine/enemy.js` 的 `?v=` 版本
2. 跑完整测试: `npx vitest run` 确认 450/15 基线
3. 报告修复结果给用户
4. 等用户浏览器实测 v1.3 Brotato 修复 (搜索中心 / 锥形 / 无推人)

### 重要 (性能优化 A 方案)
5. 实施 renderer.js 改动 (去掉普通怪 shadowBlur)
6. 实施同色批处理
7. 实施视口剔除
8. 新建 performance.test.js
9. Chrome DevTools 实测

### 讨论
10. 角色 Range 数值重设计: 现全 0, 若加角色射程加成, 参考 Brotato "近战半加成/远程全加成"

## 关键文件

- `docs/系统设计/性能预算与渲染优化方案.md`: 性能方案 A 详细
- `src/cyberblade/player.js:341, 348, 519`: v1.3 Brotato 修复
- `src/engine/enemy.js:67-70`: v1.3 无推人
- `src/cyberblade/weaponDisplay.js`: 武器详情 tier/适配
- `csv/characters.csv`: 19 角色 attackRange=0
- `csv/weapons.csv`: 47 列 (删 attackRangeMult)
- `test/unit/attack-brotato.test.js`: 16 个新测试

## 测试基线

- **当前**: 434/15 (15 fail 预存于 character.test.js)
- **本轮 v1.3 改完后**: 期望 450/15 (434 + 16 新)
- **A 方案改完后**: 期望 450+M/15 (M = performance 测试数)
